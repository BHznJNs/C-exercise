#include <stdio.h>
#include <string.h>

int lengthOfLongestSubstring(char * s){
    int length = strlen(s); // get length
    
    return 0;
}

int main() {
    char input[] = "abcabcbb";
    lengthOfLongestSubstring(input);
    return 0;
}